# Original product image folder — duplicates of assets/images/ used by the site.
# Kept locally in OneDrive as a backup; not needed in the deployed repo.
Product images/

# Instagram reference screenshots — used locally for brand voice reference, not for deployment.
Instagram/

# Sanity CMS scaffold — local experiment, not part of the deployed static site.
sanity/

# Local debug screenshots (Cloudflare deploy diagnostics, etc.) — kept locally for reference, not part of the site.
cloudflare*.jpg
cloudflare*.png

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor noise
.vscode/
.idea/
*.swp
*~

# Logs / dumps
*.log
npm-debug.log*

# Optional: dependencies (none used today, but defensive)
node_modules/

# Local-only
.env
.env.local
